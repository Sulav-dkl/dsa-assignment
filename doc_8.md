# Program: Bubble, Selection, Insertion Sorting
# a) Data Structures: **Array (`a[]})** - → Holds elements for sorting  
**Counters (`comp`, `swap`)** - - Monitor swaps and comparisons  

---

 ## b) Function and purpose

`bubble(int a[], int n, int *comp, int *swap)` → Bubble Sort sorting.

2. Selection Sort is used to sort using `selection(int a[], int n, int *comp, int *swap)`.

3. Sorts using the Insertion Sort method `insertion(int a[], int n, int *comp, int *swap)`.

---

 ## c) main()
- Reads the size of the array.  
It produces values at random.  
Selects the sorting method.  
Results and statistics are displayed.

---

 ## d) Sample Output

Prior to: 45 12 78 3 56; subsequent to: 3 12 45 56 78  
Similarities: X  
Changes: X